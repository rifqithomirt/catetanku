$(window).on("load", function () {
    /*$(".modal").modal({
        onOpenEnd: function () {
            $(".carousel.carousel-slider").carousel({
                fullWidth: !0,
                indicators: !0,
                onCycleTo: function () {
                    1 == $(".carousel-item.active").index() ? $(".btn-prev").addClass("disabled") : 1 < $(".carousel-item.active").index() && ($(".btn-prev").removeClass("disabled"), $(".btn-next").removeClass("disabled"), 3 == $(".carousel-item.active").index() && $(".btn-next").addClass("disabled"))
                }
            })
        }
    }), setTimeout(function () {
        //$(".modal").modal("open")
    }, 1800), $(".btn-next").on("click", function (e) {
        $(".intro-carousel").carousel("next")
    }), $(".btn-prev").on("click", function (e) {
        $(".intro-carousel").carousel("prev")
    })*/
    var urlLink = window.location.pathname.split('/');
    setTimeout(function(){
        $($('a[href="/'+ urlLink[1] +'/"]')[0].parentNode.parentNode.parentNode).css("display", "block");
        $($('a[href="/'+ urlLink[1] +'/"]')[0].parentNode.parentNode.parentNode).addClass("active open");
        $($('a[href="/'+ urlLink[1] +'/"]')[0]).addClass("active");
    }, 50)
    
});